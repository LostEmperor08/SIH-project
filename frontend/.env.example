# Chakravyuh SETU — frontend environment
# No fallbacks: an unconfigured build refuses to load case data rather
# than showing placeholder records.

VITE_API_URL=http://localhost:8000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
