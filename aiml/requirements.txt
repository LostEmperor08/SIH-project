# Chakravyuh SETU — AI/ML layer
# Python 3.10+

# core
numpy>=1.26,<3
pandas>=2.1
scipy>=1.11
pyyaml>=6.0

# models
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.0

# explainability
shap>=0.45

# imbalance handling
imbalanced-learn>=0.12

# serving (you already run these)
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
httpx>=0.27

# persistence
joblib>=1.3
supabase>=2.4

# ---- optional: graph neural network -------------------------------
# Installed separately — torch wheels are platform specific.
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install torch-geometric
# The pipeline detects their absence and skips the GNN stage cleanly.
